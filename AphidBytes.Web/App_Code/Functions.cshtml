@functions { 
    public static string ImageUrl(string imageName)
    {
        var versionNumber = Guid.NewGuid().ToString().Substring(0, 5);
        return string.Format("/img/{0}?v={1}", imageName, versionNumber);
    }
}
